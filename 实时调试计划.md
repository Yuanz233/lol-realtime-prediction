# LoL Esports 多赛区免费实时帧调试计划

核对时间：2026-09-19，时区：Asia/Shanghai。

## 当前可扩充规模与赛区

LoL Esports 当前 LPL 赛程页面内有40场已结束系列赛、121个已完成小局，日期覆盖2026-08-13至2026-09-13。本地已经以30秒间隔导入其中8场系列赛、31小局、1,931个采样帧，因此同一页面目前还有最多90小局可以继续核对标签并导入。训练代码和SQLite没有设置局数上限；“121局”是当前网页索引的可见范围，不是系统容量上限。扩展到其他赛区、国际赛事或更早赛季后可以继续增长，但每一局仍需取得可信的胜方和时长标签。

数据入口不限于 LPL。已经用 LCK 2026-09-13 GEN–HLE 总决赛第一局 `117030752644841644` 验证 `window` 与 `details`，返回的元数据、团队状态和选手字段与 LPL 相同；当前 LCK 页面也有 121 个已完成小局，LEC 页面有 102 个。可以把 LCK、LEC、LCS及国际赛事用于训练和实时调试，但应在数据中保存赛区与版本，并用“留出整个赛区/版本”的评估检查迁移效果，避免把跨赛区差异误认为模型泛化能力。

30秒历史采样下，31局产生1,931帧，平均约62.3帧/局；121局预计约7,500帧，普通CPU逻辑回归训练没有压力。与旧120秒数据相比，请求数量和采集时间约增加四倍。

## 帧间隔的三个概念

- LPL 历史 `window` 的一个 10 秒窗口实测包含 35 条记录，记录时间戳间隔中位数约 0.250 秒；`details` 有 60 条，中位数约 0.129 秒。LCK 首个窗口还出现多条相同或相差数毫秒的突发记录。它们是**批次内游戏状态记录密度**，不能回答直播 HTTP 响应多久发布一次。
- 批量历史采集器和单局导入器现在都默认每30秒保存一个训练点；亚秒级原始记录只用于在该时间点选择最近的真实状态，不直接作为相互独立的训练样本。
- 手机页面每 5 秒刷新一次，但页面刷新不会产生新源帧。LoL Esports 免费直播源需要同时测量“重复请求多久出现新响应”和“新响应中的最新 `rfc460Timestamp` 离接收时间多远”；定时任务将以 1 秒请求间隔连续记录至少 10 分钟。

## 直播节目与游戏时间的处理

官方赛程时间只用于发现赛事，不能作为 `game_time=0`。直播节目可能先播放选手入场、评论席、BP、设备调试或广告，这些阶段没有模型所需的局内经济和地图目标。服务按以下状态处理：

| 状态 | 判定与处理 |
| --- | --- |
| `scheduled` | 已发现官方赛事但还没到计划时间；只显示赛程，不请求胜率 |
| `preparing` | 直播已经开始，但 `window/details` 无有效帧、返回404、处于 `unstarted`，或缺少双方经济等必要字段；页面显示“准备中”，不预测、不写训练样本 |
| `in_game` | 取得队伍 ID 一致、时间戳递增且字段完整的游戏内帧后才开始预测；回取该局最早合法帧，以源 `rfc460Timestamp` 建立游戏时间轴，不能使用视频播放进度 |
| `paused` | 保持上一概率并显示暂停；暂停时长不计入游戏时间，也不生成重复训练点 |
| `waiting_next_game` | BO 系列的一小局结束后等待新的 game ID；每一小局建立独立曲线和时间零点 |
| `finished` | 收到结束状态并取得可信胜者后归档；若胜者不明确则标记待核对，不按经济领先推断 |

转播画面还可能相对遥测源有额外延迟。首轮实测会同时保存“收到帧的时间”和“源帧时间”，页面以后可展示估计数据延迟；若需要与 B 站画面严格同步，再提供可配置的显示延迟，而不修改原始训练时间轴。

## 2026-09-19 实测结果

北京时间17:05开始监测JDG–IG（match ID `116957100120526836`）。准备阶段接口先返回HTTP 204；17:14第一局`116957100120526837`出现双方经济有效的`in_game`帧。随后每秒同时请求`window`与`details`，连续600秒，共1,202次请求全部成功。`window`响应体变化间隔中位数9.999秒，批次内源帧间隔中位数0.230秒，源时间戳到接收的中位滞后77.253秒，必要队伍字段零缺失。完整方法、限制和证据见[实时帧实测](./实时帧实测_2026-09-19.md)。

免费采集器已经接入服务状态、CPU模型和SQLite，生产默认30秒保存一个点。准备、局内、暂停、局间等待和完赛状态会单独显示。该入口是网站实验接口，后续比赛仍需重新核对game ID和可用性；没有可信逐局胜者时不自动归档为训练标签。
