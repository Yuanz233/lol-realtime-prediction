# LoL Esports 网页帧：已观察字段与训练口径

观察日期：2026-09-16。以下键名来自 2026 LPL **已结束比赛**的 [团队帧窗口](https://feed.lolesports.com/livestats/v1/window/117155436343202203?startingTime=2026-09-13T09%3A30%3A00.000Z)与[详细窗口](https://feed.lolesports.com/livestats/v1/details/117155436343202197?startingTime=2026-09-12T09%3A30%3A00.000Z)的实际响应。比赛进行中的刷新频率、可访问时间、对直播画面的延迟和字段一致性尚未实测，因此这里只称“历史帧字段”。

| 位置 | 实测字段 | 首版用途 |
| --- | --- | --- |
| 顶层与帧 | `esportsGameId`、`esportsMatchId`、`gameMetadata.patchVersion`、蓝/红队 `esportsTeamId`、`frames[].rfc460Timestamp`、`gameState` | 严格对齐单局、队伍和时戳；`gameState=finished` 仅说明结束，**不表示胜者** |
| 团队窗口 `blueTeam/redTeam` | `totalGold`、`totalKills`、`towers`、`dragons`（龙种类列表）、`barons`、`inhibitors` | CPU 模型实际使用的六项蓝红差值；`dragons` 转为个数 |
| 团队窗口选手 `participants[]` | `participantId`、`totalGold`、`level`、`kills`、`deaths`、`assists`、`creepScore`、`currentHealth`、`maxHealth` | 可做之后的选手/阵容分析；首版模型暂不用，避免额外字段缺失时阻断预测 |
| 帧 `gameMetadata` 的选手映射 | 选手 ID、召唤师名、`championId`、`role` | 可显示英雄与位置；阵容特征需先验证直播时也可取得 |
| `details` 详细窗口选手 | `totalGoldEarned`、等级、K/D/A、补刀、参与率、伤害占比、放/拆眼数、攻击力/法强/攻速/护甲/魔抗、`items`、`perkMetadata`、`abilities` | 赛后详细分析候选。已读取历史结构，但**尚未证明直播时这些详细值同样可用** |

团队帧没有直接提供 `gameTime` 或逐局 `winnerTeamId`。当前历史导入用首帧与当前帧的时戳差估算比赛经过时间；遇到记录为 `gameState=paused` 的窗口，会测量暂停到恢复的间隔并从时间轴扣除。31 局的终局估算时长与独立页面公布的单局时长最多相差 8 秒。胜者来自逐局独立结果页面，并与官网赛程的队伍 ID、系列赛结果及终局时长核对；**经济领先不能充当胜者标签**。

训练只纳入结束前的采样帧，结束帧只标注真实结果并供历史曲线收尾；一局接口没有 `finished` 帧，只在独立时长核对通过后保留其最后一个**观测**帧作为训练点，概率曲线末点不伪造为 0%/100%。当前训练脚本按整个 BO 系列赛分组，把最近 20% 的系列赛留出评估，避免同一局或同一 BO 内相关帧同时进入训练和测试。批量采集默认每30秒保存一个训练点；网页上的31局历史曲线已经用同一30秒样本逻辑回归模型重算，服务默认加载 `models/live.json`，并明确标为未校准训练模型。完整采集和评估数值见[训练数据采集记录](./训练数据采集记录.md)。
