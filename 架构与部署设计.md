# 可迁移服务架构与部署设计

## 运行目标

本项目以单台低配 Windows 或 Linux 服务器为首版目标，不需要 GPU。一个 Python 进程同时提供只读网页/API和后台采集线程，CPU 逻辑回归负责推理，SQLite WAL 保存实时帧、预测概率和历史曲线。部署时从 GitHub 拉取代码，使用服务器现有 Python 创建虚拟环境，并直接在终端启动；`data/`、`models/`、`backups/` 都位于项目目录，更新代码时不会删除运行数据。

```text
LoL Esports 公开赛程页 ──> 多赛区发现与状态机
LoL Esports window 帧 ───> 字段校验 ──> CPU 模型 ──> SQLite
                                                    ├─> 手机网页
                                                    └─> JSON API
最近已完赛赛程 ──────────> 每6小时补齐曲线 ──> 待核验赛果/可训练标签
```

免费网站接口没有稳定性承诺。采集器按赛区独立运行，一个赛区的解析或网络错误不会停止其他采集线程；状态接口会逐赛区报告目标赛事、阶段、当前小局、延迟和错误。默认在赛程开始前一小时才请求比赛帧，避免全天轮询尚未开赛的 game ID。

## 数据与状态

每个帧保存以下两类信息：

- 模型输入：游戏时间、双方经济、击杀、防御塔、小龙、Baron、兵营。
- 检索元数据：`league_slug`、赛区显示名、赛事/赛季、阶段、系列赛 ID、小局 ID、计划开赛时间、源时间戳和接收时间。

旧数据库首次打开时自动新增元数据列，不删除或重算历史预测。`backfill_competition.py` 可从公开赛程页为旧对局补齐赛区、赛事和阶段。历史页依次按赛区、赛事/赛季、阶段、系列赛和小局筛选。

采集状态分为：`scheduled`、`preparing`、`in_game`、`paused`、`waiting_next_game`、`finished`。节目入场、评论席、BP和设备准备不会启动模型时间轴；双方经济有效后才建立该小局的零点。暂停时间从游戏时间中扣除。没有可信逐局胜方时，不按经济领先生成训练标签。

服务默认每6小时扫描最近14天的已完赛赛程，自动补齐遗漏的30秒曲线。直播结束但尚未取得可信逐局胜方时，曲线仍进入历史库并显示“赛果待核验”，但不会进入训练集。使用 `finalize_game.py` 提供独立赛果链接和蓝/红胜方后才成为可训练样本；补标签不会把曲线末点改成0%或100%。官方公开赛程只给系列赛总比分，帧接口只给 `finished`，两者都不足以安全推出逐局胜方。

## 模型生命周期

仓库携带已经训练的 `models/live.json`，服务器首次启动即可推理。当前生产参数使用全部46局已标注比赛、12场系列赛的2,920个30秒样本；评估仍使用按完整BO系列赛隔离的时间留出集。新增一批已核验历史对局后，先备份数据库，再运行 `train_model.py` 和 `apply_model.py`：

1. 在线备份 SQLite；
2. 重新训练 CPU 逻辑回归并输出留出集指标；
3. 用新模型重算历史曲线；
4. 在终端重启服务加载新模型。

训练仍要求至少20局、5个不同系列赛。当前46局已标注比赛是原型数据量，适合验证端到端流程，不足以证明跨版本、跨赛区概率可靠。胜者来源必须独立核验，不能从终局经济或系列赛总比分推断单局胜方。

## 服务接口

- `GET /api/health`：服务健康、进程运行时间和模型类型。
- `GET /api/status`：总状态以及每个已配置赛区的采集状态。
- `GET /api/matches`：当前、历史、超时对局及赛区/赛事目录。
- `GET /api/games/{game_id}`：单局最新资源和完整预测曲线。

接口和页面均为只读。SQLite 和模型文件不通过静态路由暴露。

## 运维边界

首版适合个人访问和少量并发。公网部署应由 Caddy/Nginx/云负载均衡终止 HTTPS，再反向代理到8000端口。若以后需要大量用户、多实例写入或长期保存数十万局，应把帧库迁移到 PostgreSQL，并把采集进程与网页进程拆开；当前 API 和帧规范可以继续使用。
