# LoL Esports 免费实时帧实测（2026-09-19）

## 结论

北京时间 2026-09-19 17:14，LPL Regional Qualifier 的 JDG–IG 第一局进入 `in_game`，LoL Esports 网站公开帧接口开始返回有效数据。连续十分钟实测共发出 1,202 次请求，`window` 和 `details` 各 601 次，全部返回 HTTP 200，未出现请求错误；这证明免费方案在本场比赛中能用于个人实时胜率观察，无需 Cito，也不需要 API key。

它不是无延迟的数据源。`window` 在本次合法查询方式下，最新源帧到本机接收时间的中位滞后为 77.253 秒，90 分位为 82.336 秒；因此网页应明确显示约一分钟以上的数据延迟。`details` 为满足接口允许的查询时间，本次按本机时间回退 510 秒取数，源帧滞后中位数为 507.156 秒，不适合作为首版实时预测主源，只作为字段研究和交叉验证。

## 对局与方法

- 系列赛：JDG vs IG，BO5
- match ID：`116957100120526836`
- 第一局 game ID：`116957100120526837`
- 正式记录区间：北京时间约 17:27:19–17:37:20
- 调用节奏：每秒同时提交一次 `window` 与一次 `details` 请求，持续 600 秒
- `window` 查询点：本机 UTC 当前时间回退 40 秒，再向下取整到 10 秒边界
- `details` 查询点：本机 UTC 当前时间回退 510 秒，再向下取整到 10 秒边界
- 记录：每次请求的时刻、响应时刻、状态码、响应哈希、源时间戳、帧数和必要字段完整性；只为变化的响应保存原始 JSON

接口要求 `startingTime` 落在 10 秒边界，并拒绝超过它所认为的广播时钟的窗口。本次回退量是现场根据错误响应给出的时钟约束确定的。它反映当前网站实现，不是稳定 API 契约。

## 测量结果

| 指标 | `window` | `details` |
| --- | ---: | ---: |
| 请求数 | 601 | 601 |
| HTTP 200 | 601 | 601 |
| 错误数 | 0 | 0 |
| 请求间隔中位数 | 0.999990 秒 | 0.999983 秒 |
| 不同响应体 | 61 | 61 |
| 响应体变化间隔中位数 | 9.999 秒 | 10.034 秒 |
| 批次内源帧间隔中位数 | 0.230 秒 | 0.172 秒 |
| 批次内源帧间隔 P90 | 1.015 秒 | 0.635 秒 |
| 源帧到接收滞后中位数 | 77.253 秒 | 507.156 秒 |
| 源帧到接收滞后 P90 | 82.336 秒 | 511.173 秒 |
| 缺少必要队伍字段的响应 | 0 | 不适用 |

每秒请求不会每秒得到新的发布批次：两端点的响应体都约每 10 秒变化一次。变化后的 `window` 响应内部包含更密的状态记录，其时间戳中位间隔约 0.23 秒。训练时不能把这些高度相关记录全部当作独立样本；项目仍按每 30 秒保存一个训练点。网页可继续每 5 秒刷新，它只展示最近已保存状态，不改变上游约 10 秒的发布节奏。

## 状态机与接入

本场 17:05 首次探测时接口返回 HTTP 204，服务将它识别为 `preparing`，不会从直播节目开始时间计算游戏时间，也不会产生预测。17:14 出现双方经济均大于零的合法 `in_game` 帧后，才建立每小局的时间零点。接入代码还区分 `paused`、`waiting_next_game` 和 `finished`；暂停时长从游戏时间中扣除，每个 game ID 建立独立曲线。

`lolesports_provider.py` 已接入现有 CPU 逻辑回归模型和 SQLite。生产默认每 30 秒轮询和保存一次，使新增数据与训练样本间隔一致；单独的实测脚本仍可用 1 秒节奏测量上游发布行为。完赛后如果公开帧没有可验证的逐局胜者，服务不会按经济领先猜胜方，需要从独立赛果页面核对后再归档训练。

## 复现与证据

```bash
python3 probe_live_feed.py 116957100120526837 \
  --seconds 600 --interval 1 \
  --window-delay 40 --details-delay 510 \
  --output data/live_probe_20260919
python3 analyze_live_probe.py data/live_probe_20260919
```

汇总结果保存在 `data/live_probe_20260919/report.json`，逐请求摘要在 `samples.jsonl`，变化响应在 `changed_responses/`。这些文件用于复核本次数据，不应据此假定未来比赛或其他赛区一定保持相同延迟和字段。
