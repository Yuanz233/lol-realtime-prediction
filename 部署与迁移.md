# GitHub 拉取与 Python 原生部署

主部署方式是把项目推送到 GitHub，再由服务器执行 `git clone` 或 `git pull`。项目不需要构建发布包或 Docker；唯一第三方运行依赖是用于 HTTPS 根证书的 `certifi`。

## 仓库中应包含的运行资产

- `models/live.json`：已经训练的生产模型；
- `data/matches.sqlite3`：当前历史比赛、原始帧和概率曲线；
- `training_manifest.json`：已人工核验的训练清单；
- `static/`、`app.py` 及其他 Python 源码；
- `requirements.txt`：安装 Windows HTTPS 校验需要的 `certifi`。

仓库不会包含 `.env`、`backups/`、`logs/`、`dist/`、SQLite WAL、临时训练数据库和实时探针抓包。不要在服务运行期间把 `matches.sqlite3-wal` 或 `matches.sqlite3-shm` 提交到 Git。

## 首次拉取

Windows PowerShell：

```powershell
Set-Location C:\Apps
git clone https://github.com/你的账号/你的仓库.git lol-realtime-prediction
Set-Location .\lol-realtime-prediction
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt
Set-ExecutionPolicy -Scope Process Bypass
.\scripts\start-windows.ps1 -Port 8000
```

Linux：

```bash
git clone https://github.com/你的账号/你的仓库.git lol-realtime-prediction
cd lol-realtime-prediction
python3 -m venv .venv
. .venv/bin/activate
python -m pip install -r requirements.txt
LIVE_PROVIDER=lolesports LOLESPORTS_LEAGUES=lpl,lck,lec,vcs,worlds \
  python app.py --host 0.0.0.0 --port 8000
```

浏览器访问 `http://服务器IP:8000`，健康检查地址为 `http://127.0.0.1:8000/api/health`。

## 配置

Windows 启动脚本默认设置：

- 实时源：`lolesports`；
- 赛区：`lpl,lck,lec,vcs,worlds`；
- 保存间隔：30秒；
- 赛程发现：120秒；
- 历史补齐：每6小时扫描最近14天；
- 手动修改赛果：关闭。

可以通过启动参数改变端口或赛区：

```powershell
.\scripts\start-windows.ps1 -Port 8080 -Leagues "lpl,lck"
```

如需更细配置，可在同一 PowerShell 会话中设置环境变量后直接运行 Python：

```powershell
$env:LIVE_PROVIDER = "lolesports"
$env:LOLESPORTS_LEAGUES = "lpl,lck,lec,vcs,worlds"
$env:ALLOW_MANUAL_RESULT = "false"
.\.venv\Scripts\python.exe .\app.py --host 0.0.0.0 --port 8000
```

## 日常更新

停止正在运行的进程，然后：

```powershell
git pull --ff-only
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\scripts\start-windows.ps1 -Port 8000
```

如果服务器数据库已经积累新比赛，不要用本地旧数据库覆盖它。更新前运行：

```powershell
.\.venv\Scripts\python.exe .\backup.py
```

备份保存在 `backups\`，该目录不会被 Git 管理。

## 训练与赛果归档

免费采集到的新比赛在无法取得可信逐局胜方时会显示“赛果待核验”。核验后可执行：

```powershell
.\.venv\Scripts\python.exe .\finalize_game.py GAME_ID --winner-side blue --label-source https://可核验的单局赛果页
```

累积一批已核验比赛后再停止服务、备份和训练：

```powershell
.\.venv\Scripts\python.exe .\backup.py
.\.venv\Scripts\python.exe .\train_model.py --db .\data\matches.sqlite3 --output .\models\live.json
.\.venv\Scripts\python.exe .\apply_model.py --db .\data\matches.sqlite3 --model .\models\live.json
```

## Git 数据冲突边界

首次部署时从仓库带上 `data/matches.sqlite3`，可以立即显示现有历史曲线。服务开始采集后，服务器数据库会成为独立运行数据。此后若本机也修改并提交同一个数据库，服务器执行 `git pull` 可能因二进制文件冲突而停止；应先备份服务器数据库，再保留服务器版本。长期运行后，更稳妥的做法是只在首次部署提交种子数据库，后续代码提交不再修改它。
