# LoL 职业比赛实时胜率预测：可行性与实施方案

调研日期：2026-09-15。目标是预测**单局（game）蓝方获胜概率**，并在手机网页展示正在进行的对局和已结束对局的预测曲线。BO3/BO5 的系列赛胜率需要另一个模型，首版不混用。

> 2026-09-16 当前决定：免费路线以 LoL Esports 多赛区网页帧为目标，默认不启用 Cito。本文的 Cito 价格、字段和适配器段落保留为早期备选方案记录；当前执行计划以[实时调试计划](./实时调试计划.md)为准。

## 结论

项目可行，瓶颈在**稳定、可授权且有足够时效的职业比赛局内数据**，而非模型推理。2026-09-15 已用免费 [LoL Esports 网页帧接口](https://feed.lolesports.com/livestats/v1/window/117155436343202203?startingTime=2026-09-13T09%3A30%3A00.000Z)实际导入四局 LPL 历史比赛的 **81 个时间帧**；2026-09-16 已扩展为 31 局、508 帧，并产生未上线的 CPU 候选模型，完整增量记录见[训练数据采集记录](./训练数据采集记录.md)。因此不购买 Cito 也有历史曲线和试验性训练路径。**免费直播抓取仍未验证**。现有自动直播采集器使用 Cito REST，是否订阅可等下一场免费源延迟实测后决定。Cito [2026-09-15 价格页](https://citoapi.com/pricing/)将 Starter 标为 **$15/月、60 秒直播延迟**，Pro 为 **$59/月、REST 实时数据**。无 Cito key 时，网页能显示已导入的历史比赛，不会填充模拟直播。

## 数据源核查

| 方案 | 实时局内字段 | 时效/访问 | 首版判断 |
| --- | --- | --- | --- |
| [Riot Developer Portal](https://developer.riotgames.com/docs/lol) | Live Client Data API 有局内信息，但接口位于**正在运行游戏的电脑** `127.0.0.1:2999`；Tournament API 的完整统计主要在赛后回调 | 普通服务器不能靠此接口任意读取职业比赛 | 不作为公共职业赛事服务的数据源 |
| [Riot 官方电竞数据 / GRID](https://riotesportsdata.com/en-us/league-of-legends/) | 官方游戏服务器直出，字段更丰富 | 商用接入 GRID；官方页面列出赛区/用途限制，LCK 标为仅媒体用途；[GRID](https://grid.gg/products/live-data/) 宣称 P95 直播数据延迟低于 200ms，但这不等于网页端到端延迟 | 更高要求时评估，必须确认本项目可获得的具体赛区与授权 |
| [Cito LoL REST](https://citoapi.com/docs/api/league-of-legends/) | `/lol/live/{gameId}/stats` 提供金币、击杀、塔、龙、Baron、兵营和选手统计；公开示例未展示局内时间、赛事 ID、队伍 ID，需用发现行和必要时的 `/visual-state` 补足 | Starter 价格页标 60 秒延迟，Pro 标实时；示例 `lagSeconds: 193` 不能视为 SLA | **当前首版**：单服务端 REST 抓取并缓存，缺字段拒绝预测 |
| [LoL Esports 网站帧窗口](https://feed.lolesports.com/livestats/v1/window/117155436343202203?startingTime=2026-09-13T09%3A30%3A00.000Z) | 实测历史职业赛帧有局内帧时戳、双方经济、击杀、塔、龙、Baron、兵营、队伍 ID；2026 LPL 31 局已导入 508 帧 | 本次只读请求无需 key；网站展示接口，不是 Developer Portal 稳定职业 API；直播刷新/延迟、请求限制待测 | **当前免费历史导入**：可供曲线和试验性训练，但每局胜者需独立核对 |
| [B 站赛事中心](https://www.bilibili.com/match/game?gid=2&sid=787) | 赛程、系列赛比分和单局详情入口；本次历史详情数据区加载失败 | 无正在进行的目标赛事可测；逐帧经济及历史留存尚未证实 | 观赛/赛程入口，暂不作模型帧源 |
| [PandaScore Live API](https://developers.pandascore.co/docs/plan-reference) | Live Basic 帧提供时钟、胜者、蓝红双方金币、击杀、塔、龙、Baron、兵营等 | [WebSocket 帧约每 2 秒](https://developers.pandascore.co/docs/websockets-overview)，与公开直播同步 | 高价备选，当前不接入 |
| [Oracle's Elixir](https://master.d36liwrx5rvjnc.amplifyapp.com/tools/downloads) | 职业比赛 CSV，适合赛后统计、队伍先验 | 下载表通常是赛后/检查点数据，不是 2 秒级完整局内帧 | 可补赛前先验；不能独立训练/驱动首版局内曲线 |

**价格与覆盖**：[PandaScore 价格页](https://www.pandascore.co/pricing)标示免费套餐只含日程/结果，Live Basic 从 €1,000/游戏/月起（未含税），月订阅为一个月合同；实际报价由选定赛事覆盖决定。公开文档无法确认 LPL/LCK 等每个赛事的逐场直播覆盖与网页再展示权，上线前必须核对合同/测试 token。`live_supported` 也要按赛事检查。PandaScore 的 [`/lol/games/{id}/frames`](https://developers.pandascore.co/reference/get_lol_games_lolgameid_frames) 可获取赛后帧，最低权限为 Live Basic；可用于训练与历史曲线回填。已接入期间也可以在本地保存每个实时帧。

## 模型调研与选择

### 预算为零时的试验路径

- [Cito Free](https://citoapi.com/pricing/)提供每月 500 次免费测试请求，并有 [LoL 实时局内面板接口](https://citoapi.com/docs/api/league-of-legends/)；按 2 秒一帧，30 分钟一局约 900 次请求，免费额度不适合持续直播。它是独立聚合商，明确说明其数据不是 Riot 官方或已清权数据。
- [GRID Open Access](https://grid.gg/open-access/)确有免费计划，但当前公开页面明确列出的游戏是 CS2 和 Dota 2，不是 LoL。其 [LoL Data Portal](https://grid.gg/get-league-of-legends/)把职业战队标为免费、普通产品/其他用途标为付费；不能把战队权限当作公开网页权限。
- Riot 官网用于展示的电竞局内接口不是 [Riot Developer Portal](https://developer.riotgames.com/docs/lol)公布的、面向第三方服务器的稳定职业赛事 API。但本项目已实际取得 2026 LPL 历史比赛逐帧数据并写入 SQLite；下一步测直播更新和批量逐局结果。正式部署前不能假定可长期调用或获准转展示。
- 免费的 [Oracle's Elixir 比赛 CSV](https://master.d36liwrx5rvjnc.amplifyapp.com/tools/downloads)可用于赛前先验与赛后静态统计；不能提供首版所需的持续局内帧。

如果预算必须为零，现在可先用 `import_lolesports_history.py` 导入历史职业赛、积累标签训练并展示手机网页；自有电脑运行无需新增订阅费。Cito 免费额度每月 500 次，持续全天发现和抓帧会很快超额。当前代码已接入 **LoL Esports 历史帧导入**与 **Cito 自动直播采集**，尚未接入免费直播自动发现/采集。

- [2023 年研究](https://arxiv.org/abs/2309.02449)比较了多个 LoL 比赛进程预测模型：LightGBM 在其特定数据、60%–80% 比赛进度区间有 81.62% 分类准确率；早期阶段逻辑回归/梯度提升更有效。论文数据并非现成可直接部署的职业赛事权重，准确率也不能当作本项目结果。
- [开源 `lol_predictor`](https://github.com/Jimonhitori/lol_predictor)有职业比赛局内特征、历史帧回填、逻辑回归训练和按 game 切分的流程，可参考特征与验证；其数据接口/权重与本项目供应商字段及授权需重新核实，不直接将它当成即插即用生产模型。
- 首版实现**CPU 可训练的正则化逻辑回归**，只使用已在免费历史帧实测可取、且在 Cito 文档中也出现的蓝红双方差值：金币、击杀、防御塔、龙、Baron、兵营，以及赛中时间。LoL Esports 帧的时间由首帧时戳估算，Cito 则要求返回明确局内时间，两种口径必须核对后才混合训练/上线。推理为少量乘加与 sigmoid，通常远小于 30 秒默认抓取间隔；实际耗时要在目标服务器测量。样本不足时只显示“未训练、未校准的实验性基线”。
- 已用明确逐局胜者的 31 局历史帧训练 CPU 候选模型，按**整个 BO 系列赛分组、时间顺序**留出评估，防止相关帧泄漏；留出的 9 局得到 Brier score 0.1269、log loss 0.3943。仍需更多不同赛事/版本来检验按时间段可靠性、校准曲线、赛事漂移；若数据量足够，再比较 LightGBM 与简单模型，并在验证集校准概率。免费网页帧和 Cito 在线帧要核对时间/字段口径后才可共用模型。

## 字段和延迟预算

输入：`game_id`、`match_id`、`current_timestamp`，蓝红双方 `id/name`、`gold/kills/towers/drakes/nashors/inhibitors`、`finished/winner_id`。团队先验、英雄阵容、补刀可后续加入，但所有用于训练的字段必须在在线帧同样存在；避免只在 Pro 套餐、赛后结算或未来帧才出现的字段。

数据新鲜度要同时看**供应商数据帧时间、报告延迟和服务端收到时间**。Starter 的公开套餐说明为 60 秒延迟；后端默认每 30 秒抓取，前端每 5 秒刷新，因此页面重复读取同一帧是正常现象，不能称为 5 秒实时预测。页面展示帧时间/延迟，120 秒无新帧的未完赛对局转为超时列表。服务端保留每局按实际游戏时间排序的点，用于历史曲线。

## 首版架构与交付范围

直播路径：`Cito /lol/live -> 每局 REST /stats（缺局内时间时再取 /visual-state）-> 校验 -> SQLite 帧与概率 -> HTTP JSON API -> 手机网页`。免费历史路径：`已核对的 LoL Esports 单局 ID + 逐局胜者 -> 网站 window 帧 -> 每分钟采样/校验 -> 同一 SQLite -> 历史网页与 CPU 训练`。

本仓库脚本实现：设置 Cito key 后发现 LoL 对局并定时抓取；免费历史网页帧导入、离线 JSONL 导入、局内胜者赛后对账、逻辑回归训练入口；单局蓝方概率和双边统计；进行中、历史、超时列表与曲线。**旧比赛完整逐帧回放没有从 Cito 公开文档确认**，但 LoL Esports 网站历史帧已实测可用；网页只展示实际摄取的对局，未得到明确胜者时不归档。首版单进程 SQLite 适合小规模服务，公网部署建议反向代理 HTTPS、服务管理器和备份。

## 上线前必须验证

1. 下一场目标赛事开打时验证免费网页帧的**游戏 ID 发现、直播更新、延迟、缺帧和完赛标签**；本次只完成历史实测。
2. 已批量导入 31 局并用独立逐局结果核对胜者，按整个 BO 系列赛做时间外评估；还需扩充不同赛事与版本并完成概率校准，20 局最低门槛仅用于验证训练流程。
3. 若免费实时源不满足个人使用，再决定是否开 Cito 套餐；开通后用真实 `/lol/live`、`/stats`、`/visual-state` 和一场完整比赛检查 ID、局内时间、球队字段、胜者格式及切局。当前 Cito 适配器只有离线假数据测试。
4. 公网网页部署前核对数据接口可持续访问、目标赛区覆盖和展示用途，并在目标手机网络测量页面实际滞后。

## 主要来源

[PandaScore 套餐字段](https://developers.pandascore.co/docs/plan-reference) · [WebSocket 与直播同步说明](https://developers.pandascore.co/docs/websockets-overview) · [LoL 帧字段](https://developers.pandascore.co/docs/data-sample-league-of-legends) · [认证与 token 保密](https://developers.pandascore.co/docs/authentication) · [限制](https://developers.pandascore.co/docs/rate-and-connections-limits) · [官方电竞数据](https://riotesportsdata.com/en-us/league-of-legends/) · [Riot 普通 API](https://developer.riotgames.com/docs/lol) · [模型研究](https://arxiv.org/abs/2309.02449)
