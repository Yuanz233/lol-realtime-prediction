# 免费职业比赛帧源验证：B 站与 LoL Esports

验证日期：2026-09-15。目标是个人使用、普通 CPU 训练、手机网页展示单局胜率曲线。本页保留首次四局、81 帧的验证记录；2026-09-16 扩大到 31 局、508 帧及候选模型，见[训练数据采集记录](./训练数据采集记录.md)。这里不把赛程比分当成逐帧训练数据，也不把历史接口可用推定为直播可用。

## 结论

**历史帧已证实可免费取得，项目的训练数据可行性并未消失。**2026-09-13 LPL 总决赛 BLG–AL 的四小局均从 LoL Esports 网页帧接口读到了首帧和 `gameState=finished` 的终局帧，逐局胜者也经独立页面核对。第一局的 [网页帧窗口](https://feed.lolesports.com/livestats/v1/window/117155436343202203?startingTime=2026-09-13T09%3A30%3A00.000Z) 在未提供 API key 的只读请求中返回了双方金币、击杀、塔、龙、Baron、兵营和帧时间。项目脚本已把**四局共 81 个真实帧**导入本地历史页；CPU 训练入口识别出四局共 77 个赛中采样点。

这不是 [Riot Developer Portal](https://developer.riotgames.com/docs/lol)公布的稳定职业赛事 API，而是 LoL Esports 网站用于展示赛事的接口。它目前对这场历史比赛免费可读，不代表长期可用、无限请求、覆盖所有赛事，也不证明可以把数据公开再分发。2026-09-19 JDG–IG 第一局已进一步完成十分钟直播实测：免费取得 game ID 和游戏内帧，`window` 601次请求全部成功，响应约每10秒变化，源时间戳到接收的中位滞后约77秒。

## B 站主页能提供什么

用户给的 [B 站英雄联盟赛事主页](https://www.bilibili.com/match/game?gid=2&sid=787) 可见赛程、系列赛比分、回放和“数据”入口。2026-09-13 BLG–AL 显示 **1:3**，这只是 BO5 总比分，不能直接作为每小局训练标签。其 [历史单局详情](https://www.bilibili.com/v/game/match/singledata/37226) 有“第一局”等标签，但本次打开时“赛程数据”主体没有载入，浏览器报错 `Cannot read properties of undefined (reading 'data')`。因此**本次没有证实 B 站能提供历史逐帧经济/击杀曲线**，也没有观察到一场正在进行的比赛来确认是否逐帧推送经济。

B 站 2023 年 [ESG 报告](https://ir.bilibili.com/media/jj4cmyl1/bilibili-2023-environmental-social-and-governance-report-c.pdf)提到 S13 直播间“实时数据功能”，但未在这次验证中确认其字段、接口、历史留存或延迟。直播视频里的经济数字即使可看见，也不能自动等同于服务器可低成本、稳定获取的结构化时间帧。B 站可作为赛程/观赛入口，暂不作为本项目的训练帧源。

## 历史帧与胜者标签的实测

| 项目 | 本次观察 |
| --- | --- |
| 单局 ID | `117155436343202203`；返回 `esportsMatchId=117155436343202202` |
| 起止帧 | `2026-09-13T09:06:23.066Z` 至 `09:36:35.264Z`；初始窗口金币为 0，随后取非零赛中帧 |
| 09:30 附近 | 蓝方金币约 41,748、击杀 5；红方金币约 43,423、击杀 9；同帧有塔、龙等字段 |
| 队伍 ID | 帧元数据蓝方 `99566404853854212`（BLG）、红方 `99566404856367466`（AL） |
| 单局胜者 | 独立 [Games of Legends 第一局页面](https://gol.gg/game/stats/82967/page-game/)标 BLG `LOSS`、AL `WIN`，因此这一局红方胜。该页面标游戏时长 `30:07`，与帧时戳跨度约 `30:12` 接近。 |
| 导入结果 | 第一局 SQLite 31 帧、历史曲线 31 点、训练入口 30 个赛中帧；最后一点按明确胜者显示蓝方 0%。此前赛中点仍标为**实验性基线**，不能解释成已经训练好的胜率。 |

帧字段里没有直接的 `gameTime`；脚本以首帧时戳计算“比赛已过秒数”。本次与独立统计的游戏时长相差约 5 秒，作为首版曲线时间轴足够接近，但在训练模型或与 Cito 在线帧混用前应再抽查更多比赛的时间偏差。

其余三局也完成首末窗口、队伍 ID 和胜者核对，且已导入历史曲线。第二至四局为每 120 秒取样；第一局每 60 秒取样。逐局胜者合计 AL 3、BLG 1，与 B 站系列赛 1:3 一致：

| 局 | LoL Esports game ID | 帧里的蓝方/红方 | 独立单局结果 | 历史点数 |
| --- | --- | --- | --- | ---: |
| 1 | `117155436343202203` | BLG / AL | [AL 胜](https://gol.gg/game/stats/82967/page-game/) | 31 |
| 2 | `117155436343202204` | AL / BLG | [AL 胜](https://gol.gg/game/stats/82968/page-game/) | 16 |
| 3 | `117155436343202205` | AL / BLG | [BLG 胜](https://gol.gg/game/stats/82969/page-game/) | 18 |
| 4 | `117155436343202206` | BLG / AL | [AL 胜](https://gol.gg/game/stats/82970/page-game/) | 16 |

LoL Esports 网站的赛程/赛事详情接口可用于查系列赛和 game ID，但读取它们需要网站使用的 `x-api-key`，并非可直接使用的个人 Developer Portal key。此次验证中赛事详情只给了系列赛 3:1，没有可靠的逐局胜者；还发现详情里的第一局方位与帧元数据方位不一致。导入脚本因此以**帧自身的蓝红队伍 ID**为准，要求操作者另行核对并输入**每一小局胜者**，绝不从领先经济或系列赛比分推断胜者。脚本不内置、保存或展示网站 key。

## 已实现的免费历史导入

本项目的 [import_lolesports_history.py](./import_lolesports_history.py)直接取公开帧窗口，不需要 Cito 订阅或 GPU。它按30秒采样、要求数据里的 game/match/队伍 ID 一致、拒绝缺失字段或未结束的比赛，并保存胜者来源。网页继续用原有 SQLite 历史曲线。

```bash
python3 import_lolesports_history.py 117155436343202203 \
  --blue-name BLG --red-name AL --winner-side red \
  --label-source https://gol.gg/game/stats/82967/page-game/
python3 app.py
```

网页默认打开 `http://127.0.0.1:8000`。当前项目数据库已导入上述真实四小局，可立即在“历史”查看。此接口实测 `startingTime` 必须落在 **10 秒窗口边界**，任意秒数曾返回 HTTP 400；脚本已按窗口对齐。每局约30分钟、30秒取样需要约62次读请求，20局约1,240次；网站没有向本项目承诺请求额度，应控制抓取频率并保留失败重试能力。

训练脚本最低需 **20 局带可靠单局胜者的完整比赛**，且会按整局时间顺序留出 20% 评估；本次四局验证了真实训练输入，却不足以训练可用模型，而且同一 BO5 的四局彼此相关，后续必须覆盖更多对阵、赛区与版本。批量样本还需要 game ID 列表、逐局胜者对照和抽样复核。导入时应保留 `--label-source`，把人工标注的出错风险降到可追溯。采集够样本后执行 `python3 train_model.py`，检查 log loss、Brier score 与按时间段的校准，再决定是否在网页上展示训练概率。

## 个人使用的成本与下一步

历史帧这条已实测路径的数据订阅费为 **$0**。自用并在已有电脑上运行时也无需新增服务器费；若需持续从手机外网访问，才有 VPS、域名或网络成本。CPU 逻辑回归训练/推理不需要 GPU。Cito 可以保留为**自愿购买的稳定数据源候选**，不再是验证历史训练可行性的前提。

直播可用性已在一场LPL比赛中得到验证，完整结果见[2026-09-19实时帧实测](./实时帧实测_2026-09-19.md)。这仍不能保证“永久免费”：网站接口可能变更，其他赛区可能不给帧，当前约77秒的滞后也不是零延迟。后续比赛应继续监控字段、延迟、断流以及完赛帧和逐局胜者何时出现；历史训练与曲线能力不依赖直播持续可用。
