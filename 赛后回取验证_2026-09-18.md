# 2026-09-18 WE–JDG 赛后回取验证

核对时间：2026-09-18 23:14（Asia/Shanghai）。本记录只验证赛后回取，不把结果描述成直播实时性验证。

## 比赛与接口

LoL Esports LPL 赛程页已将本场标为结束：WE 1:3 JDG，match ID 为 `116957100120526830`；四个已完成的小局 ID 为 `116957100120526831` 至 `116957100120526834`。页面内这四局的 `vods` 均为空，赛事的 `streams` 也为空，因此该结构化赛程响应没有提供可直接使用的视频回放链接。

项目当前不读取任何 B 站、YouTube 或其他视频流。实际使用的是：

- 赛程和 ID 发现：`https://lolesports.com/en-US/leagues/lpl`
- 团队统计帧：`https://feed.lolesports.com/livestats/v1/window/{game_id}`
- 选手详细帧：`https://feed.lolesports.com/livestats/v1/details/{game_id}`

## 已验证结果

第一局 `116957100120526831` 的 `window` 在赛后仍可无 API key 读取。默认窗口返回10帧，时间为 `2026-09-18T09:12:22.261Z` 至 `09:12:23.632Z`，状态为 `in_game`，蓝方 WE、红方 JDG。请求晚于比赛结束的窗口后，返回 `09:48:33.670Z` 至 `09:48:37.448Z` 的终局尾部，包含 `in_game` 和 `finished`。`details` 在 `09:20:00Z` 的十秒窗口返回47个详细帧，包含装备、符文、技能、等级、KDA、补刀和伤害占比等字段。

这证明当天比赛结束后仍能回取结构化历史帧，可用于：验证 game/match/队伍 ID、字段完整性、生成胜率曲线、检查模型在新比赛上的输出，以及配合可信的逐局胜者标签扩充训练集。

赛后回取不能验证以下直播指标：比赛进行时端点何时首次可用、HTTP 响应多久更新一次、源帧相对直播画面的延迟、直播期间是否断流或限流。视频录像可以用于人工核对事件和逐局胜者，也不能还原当时服务器发布响应的时间。因此实时可用性仍必须在比赛进行中连续请求并记录接收时间。
